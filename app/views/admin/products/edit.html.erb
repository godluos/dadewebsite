<!--=== 編輯商品 ===-->
<h2 class="admin-panel-title col-md-8 col-md-offset-1"><span class="fa fa-pencil-square-o"></span> <%= t('编辑商品') %></h2>
<%= render('form') %>

<!-- 原代码 -->
<!-- <h2> Edit Product </h2>
<%= simple_form_for [:admin, @product] do |f| %>
  <div class="group">
    <%= f.input :title %>
  </div>
  <div class="group">
    <%= f.input :description %>
  </div>
  <div class="group">
    <%= f.input :price %>
  </div>
  <% if @product.image.present? %>
    <span>目前商品图</span> <br>
    <%= image_tag(@product.image.thumb.url) %>
  <% end %>
  <div class="group">
    <%= f.input :image, as: :file %>
  </div>
  <div class="group">
    <%=f.select :category_id, @categories %>
  </div>
  <%= f.input :is_hidden %>
  <%= f.submit "Submit", data: { disable_with: "Submitting..." } %>
<% end %> -->
<!-- 原代码 end -->
